version: '3.4'

services:
  app:
    environment:
      ENV: localdev
      PORT: 8000
      AUTH: SAML_MOCK
    restart: always
    container_name: app-canvas-analytics
    build:
      context: .
      target: app-container
    volumes:
      - ./data_aggregator:/app/data_aggregator
    ports:
      - "8000:8000"
